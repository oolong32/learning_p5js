function setup() {
	createCanvas(400, 600);
	resetSketch();
	// create button to reset the sketch  
	var button = createButton("reset");
	button.mousePressed(resetSketch);
}

// define function to reset Sketch
function resetSketch() {
	// Everything associated with starting the sketch except createCanvas
	// x y z
}

function draw() {
  
}
